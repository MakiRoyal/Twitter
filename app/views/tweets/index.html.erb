<section class="section">
  <div class="container">
    <div class="columns">
      <%= render "trends" %>
      <%= render 'feed' %>
      <%= render 'whoToFollow' %>
    </div>
  </div>
</section>


<div id="tweets">
  <% @tweets.each do |tweet| %>

  <% end %>
</div>

